"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const commands_1 = require("../../commands");
const PeriodBasedReport_1 = require("../../../base/PeriodBasedReport");
const vorpal = require('../../../../vorpal-init');
class SpoReportSiteUsageFileCountsCommand extends PeriodBasedReport_1.default {
    get name() {
        return `${commands_1.default.REPORT_SITEUSAGEFILECOUNTS}`;
    }
    get usageEndpoint() {
        return 'getSharePointSiteUsageFileCounts';
    }
    get description() {
        return 'Get the total number of files across all sites and the number of active files';
    }
    commandHelp(args, log) {
        log(vorpal.find(this.name).helpInformation());
        log(`  Remarks:

    A file (user or system) is considered active if it has been saved, synced,
    modified, or shared within the specified time period.
    
  Examples:
      
    Get the total number of files across all sites and the number of active
    files for the last week
      ${commands_1.default.REPORT_SITEUSAGEFILECOUNTS} --period D7

    Get the total number of files across all sites and the number of active
    files for the last week and exports the report data in the specified path in
    text format
      ${commands_1.default.REPORT_SITEUSAGEFILECOUNTS} --period D7 --output text --outputFile "siteusagefilecounts.txt"

    Get the total number of files across all sites and the number of active
    files for the last week and exports the report data in the specified path in
    json format
      ${commands_1.default.REPORT_SITEUSAGEFILECOUNTS} --period D7 --output json --outputFile "siteusagefilecounts.json"
`);
    }
}
module.exports = new SpoReportSiteUsageFileCountsCommand();
//# sourceMappingURL=report-siteusagefilecounts.js.map